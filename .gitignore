# machine-local / company-specific — never commit
config/nvim/lua/gearup_local.lua
LOCAL_NOTES.md

# compiled demo binaries
demo/services/users/users
demo/services/orders/orders

# local TUI build artifact (make build); the installed copy lives in ~/.local/bin
/gearup

# GoReleaser output (make snapshot)
/dist/

# editor droppings
*.swp
.DS_Store
